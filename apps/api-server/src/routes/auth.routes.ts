import { Router } from "express";

const router = Router();

// TODO: POST /register — Create a new user account
// TODO: POST /login    — Authenticate and return JWT
// TODO: GET  /me       — Return the currently authenticated user

export default router;
