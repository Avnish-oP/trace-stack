import { Router } from "express";

const router = Router();

// TODO: GET    /             — List projects for the current organisation
// TODO: POST   /             — Create a new project
// TODO: GET    /:projectId   — Get project by ID
// TODO: PATCH  /:projectId   — Update project
// TODO: DELETE /:projectId   — Delete project

export default router;
