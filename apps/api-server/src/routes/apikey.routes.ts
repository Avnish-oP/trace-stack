import { Router } from "express";

const router = Router();

// TODO: GET    /          — List API keys for a project
// TODO: POST   /          — Create a new API key
// TODO: DELETE /:keyId    — Revoke / delete an API key

export default router;
